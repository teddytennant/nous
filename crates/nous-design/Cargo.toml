[package]
name = "nous-design"
description = "Editorial design tokens for Nous — palette, typography, spacing. Shared across Rust surfaces (TUI, CLI)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
ratatui = { version = "0.29", optional = true }

[features]
default = []
ratatui = ["dep:ratatui"]
